// Import dependencies
import styled from '@emotion/styled'

// Styles

// Config Container
export const ConfigContainer = styled.div`
    width: 95%;
    margin: 12vh auto 0 22vw;
    padding: 40px 0 0 0;
    background-color: var(--font-color);

    & > h1 {
        color: var(--dark-color)
    }
`